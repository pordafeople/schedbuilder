import React from 'react'
import { ScheduleTable } from '../process/arrange'

function Schedules({ table }: ScheduleTable) {
    console.log(table);
    return(
        <h1>hello world</h1>
    )
}

export default Schedules
